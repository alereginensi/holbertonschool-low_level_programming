#include "main.h"
#include <stdio.h>
#include <string.h>


/**
 * _puts - prints a string, followed by a new line, to stdout.
 * @str: variable
 *
 * Return: Always 0.
 */
void _puts(char *str)
{
	puts(str);
}
