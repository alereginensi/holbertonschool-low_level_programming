#include "main.h"
/**
 * _isalpha - jdsjsdj
 *
 * @letter: sdjsadka
 *
 * Return: Always 0 (Sucess)
 **/
int _isalpha(int letter)
{

  if (letter >= 'a' && letter <= 'z')
    {
      return (1);
    }
  else if (letter >= 'A' && letter <= 'Z')
    {
      return (1);
    }
  else
    {
      return (0);
    }
}
