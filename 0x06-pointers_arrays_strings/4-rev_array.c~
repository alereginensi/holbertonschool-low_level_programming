#include "main.h"
#include <stdio.h>

/**
 * reverse_array - function that concatenates two strings
 * @a: variable
 * @n: variable
 *
 * Return: Always 0.
 */
void reverse_array(int *a, int n)
{
	int i, j, k;


	for (i = 0; i < n; i++)
	{
		n = a[i];
		a[i] = *a;
		*a = n;
		a--;
	}

}
