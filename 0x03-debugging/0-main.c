#include "main.h"

/**
 * * main - tests function that prints if integer is positive or negative
 * * Return: 0
 * */

int main(void)
{
	int i;
	i = 0;
	positive_or_negative_or_zero(i);

	return (0);
}
