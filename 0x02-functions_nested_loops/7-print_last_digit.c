#include "main.h"
/**
 * print_last_digit - jdsjsdj
 *
 * @i: sdjsadka
 *
 * Return: Always 0 (Sucess)
 **/
int print_last_digit(int i)
{
	int last = i % 10;

	if (i < 0)


	last = last * -1;

	_putchar(last + '0');

	return (last);
}

