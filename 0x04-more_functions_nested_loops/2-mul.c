#include "main.h"
#include <stdio.h>

/**
 * mul - function that multiplies two integers.
 * @a: ch
 * @b: ch
 *
 * Return: Always 0.
 */
int mul(int a, int b)
{
	int result;

	result = (a * b);
	return (result);
}
